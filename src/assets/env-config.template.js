// Copy this file to env-config.js (already gitignored) and fill in a real key
// to test the location picker's Google Map locally. In CI, this file is
// generated from the GOOGLE_MAPS_API_KEY repo secret before the build runs
// (see .github/workflows/azure-static-web-apps-*.yml) — never commit the real key.
window.__env = {
  googleMapsApiKey: '',
};
